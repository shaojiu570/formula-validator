# 🚀 GitHub Pages 部署指南

## 📋 前提条件

1. 已经在 GitHub 创建了仓库：`https://github.com/shaojiu570/formula-validator.git`
2. 本地已经安装了 Node.js 和 npm
3. 本地已经安装了 Git

## 🎯 部署步骤

### 方法一：使用部署脚本（推荐）

#### Windows 用户
```bash
# 双击运行或在命令行执行
deploy-to-github.bat
```

#### Mac/Linux 用户
```bash
# 添加执行权限
chmod +x deploy-to-github.sh

# 运行脚本
./deploy-to-github.sh
```

### 方法二：手动部署

```bash
# 1. 使用 GitHub Pages 配置构建
npx vite build --config vite.config.github.js

# 2. 进入构建目录
cd dist

# 3. 初始化 Git
git init
git add -A
git commit -m "Deploy to GitHub Pages"

# 4. 推送到 gh-pages 分支
git push -f https://github.com/shaojiu570/formula-validator.git master:gh-pages

# 5. 返回项目根目录
cd ..
```

## ⚙️ GitHub Pages 设置

部署后，需要在 GitHub 仓库中配置 Pages：

1. 访问：https://github.com/shaojiu570/formula-validator/settings/pages

2. 在 "Source" 部分：
   - 选择 **"Deploy from a branch"**
   - Branch: 选择 **"gh-pages"**
   - Folder: 选择 **"/ (root)"**

3. 点击 **"Save"** 保存

4. 等待几分钟，GitHub Pages 会自动部署

## 🌐 访问地址

部署成功后，访问：
**https://shaojiu570.github.io/formula-validator/**

## 🔍 常见问题

### Q1: 404 错误
**原因**：GitHub Pages 设置不正确或分支不存在

**解决**：
1. 确认 `gh-pages` 分支已创建
2. 检查 GitHub Pages 设置是否选择了 `gh-pages` 分支
3. 等待几分钟让 GitHub Pages 生效

### Q2: 页面空白
**原因**：资源路径不正确

**解决**：
1. 确保使用了 `vite.config.github.js` 配置构建
2. 检查浏览器控制台的错误信息
3. 确认 `base: '/formula-validator/'` 配置正确

### Q3: 样式丢失
**原因**：CSS 文件路径错误

**解决**：
1. 重新使用 GitHub 配置构建
2. 清除浏览器缓存
3. 强制刷新页面（Ctrl+F5）

### Q4: 推送失败
**原因**：网络问题或权限问题

**解决**：
1. 检查网络连接
2. 确认 GitHub 账号有仓库的写权限
3. 尝试使用 SSH 方式推送

## 📝 配置说明

### 本地开发配置（vite.config.js）
```javascript
base: './'  // 相对路径，用于本地开发和直接打开
```

### GitHub Pages 配置（vite.config.github.js）
```javascript
base: '/formula-validator/'  // 绝对路径，用于 GitHub Pages
```

## 🔄 更新部署

每次修改代码后，重新运行部署脚本即可：

```bash
# Windows
deploy-to-github.bat

# Mac/Linux
./deploy-to-github.sh
```

## 💡 提示

1. **首次部署**需要在 GitHub 设置 Pages 源为 `gh-pages` 分支
2. **后续更新**只需运行部署脚本
3. **部署需要时间**，通常 2-5 分钟后生效
4. **本地开发**仍然使用 `npm run dev`，不受影响

## 🎯 部署流程图

```
本地修改代码
    ↓
运行部署脚本
    ↓
使用 GitHub 配置构建
    ↓
推送到 gh-pages 分支
    ↓
GitHub Pages 自动部署
    ↓
访问网站
```

## 📞 获取帮助

如果遇到问题：
1. 检查浏览器控制台的错误信息
2. 查看 GitHub Actions 的部署日志
3. 确认 GitHub Pages 设置正确
4. 尝试清除浏览器缓存

---

**重要**：本地开发使用 `npm run dev`，GitHub 部署使用 `deploy-to-github` 脚本！